# Database Configuration
DB_HOST=localhost
DB_NAME=db_pengaduan_sekolah
DB_USER=root
DB_PASS=
DB_PORT=3306

# Aiven / SSL Configuration
# Set to 'true' to enable SSL
DB_SSL=false
# Optional: Path to CA certificate (Aiven often doesn't need this if using public CA)
# DB_SSL_CA_PATH=/path/to/ca.pem
